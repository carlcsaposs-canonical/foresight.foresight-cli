{
  "name": "@thundra/root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "lerna exec npm install",
    "compile": "lerna exec npm run compile",
    "diff": "lerna diff",
    "publish": "lerna exec -- npm publish"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
